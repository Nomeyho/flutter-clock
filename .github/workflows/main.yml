on: push
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      # Setup
      - uses: actions/checkout@v1
      - uses: actions/setup-java@v1
        with:
          java-version: '13.x'
      - uses: subosito/flutter-action@v1
        with:
          channel: 'beta'
      - run: flutter config --enable-web
      - run: flutter devices
      # Build
      - run: |
          cd nomeyho_clock
          flutter pub get
      - run: |
          cd nomeyho_clock
          flutter analyze
      - run: |
          cd nomeyho_clock
          flutter build web
      # Publish
      - run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add nomeyho_clock/build/web -f
          git commit -m "Update page"
      - uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}

#
#  release:
#    runs-on: ubuntu-latest
#    steps:
#      # Setup
#      - uses: actions/checkout@v1
#      - uses: actions/setup-java@v1
#        with:
#          java-version: '13.x'
#      - uses: subosito/flutter-action@v1
#        with:
#          channel: 'stable'
#      # Release
#      - use: rm nomeyho_clock/ios
#      - use: rm nomeyho_clock/android
#      - use: rm nomeyho_clock/web
#      - use: cd nomeyho_clock && flutter clean
#      - use: zip -r nomeyho_clock.zip ./nomeyho_clock ./flutter_clock_helper
#      # Artifacts
#      - uses: actions/upload-artifact@v1
#        with:
#          name: nomeyho_clock.zip
#          path: nomeyho_clock.zip